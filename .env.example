# Database Configuration
# For teammates: Change DB_HOST to the IP address of the machine running the database

# Use this on the host machine (where database is running):
# DB_HOST=localhost

# Use this on teammate machines (replace with actual host IP):
# DB_HOST=192.168.1.123

DB_NAME=csit321_face_recognition
DB_USER=postgres
DB_PASSWORD=joshua1102
DB_HOST=localhost
DB_PORT=5432
